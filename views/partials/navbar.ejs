<nav class="bg-zinc-900 text-white py-4 shadow-md mb-6 border-b border-orange-500">
  <div class="max-w-6xl mx-auto px-4 flex justify-between items-center">
    <a href="/" class="text-2xl font-bold text-orange-400">MiniProject</a>
    <div class="space-x-4">
      <% if (typeof user !== 'undefined') { %>
        <a href="/profile" class="hover:text-orange-400">My Profile</a>
        <a href="/logout" class="hover:text-orange-400">Logout</a>
      <% } else { %>
        <a href="/login" class="hover:text-orange-400">Login</a>
      <% } %>
    </div>
  </div>
</nav>
